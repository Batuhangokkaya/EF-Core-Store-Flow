@model List<CustomerCityGroup>
@{
    ViewData["Title"] = "Müşteri Şehir Grub Listesi";
    Layout            = "~/Views/Layout/Index.cshtml";

    int count = 0;
}

<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <div>
                        <h3 class="mb-1">Şehirlere Göre Müşteri Sayıları</h3>
                        <p class="text-muted mb-0">
                            Müşteriler şehir bilgilerine göre gruplanmış ve her şehirdeki toplam müşteri sayısı gösterilmektedir.
                        </p>
                    </div>
                    <span class="badge badge-primary p-2">
                        @Model.Count Şehir
                    </span>
                </div>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th style="width: 45%;">#</th>
                                <th style="width: 45%;">Şehir Adı</th>
                                <th>Müşteri Sayısı</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var customerCityGroup in Model)
                            {
                                count++;
                                <tr>
                                    <td>@count</td>
                                    <td>@customerCityGroup.City</td>
                                    <td>@customerCityGroup.CustomerCount</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>